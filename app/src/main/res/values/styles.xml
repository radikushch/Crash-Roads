<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyEditText" parent="@android:style/TextAppearance.Widget.EditText">
        <item name="android:maxLines">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="MyEditText.Title" parent="MyEditText">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:color">@color/colorTitle</item>
    </style>

    <style name="MyEditText.Simple" parent="MyEditText">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:color">@color/colorText</item>
    </style>

    <style name="MyEditText.Title.NotUnderline" parent="MyEditText.Title">
        <item name="android:background">@color/fui_transparent</item>
    </style>

    <style name="MyEditText.Simple.NotUnderline" parent="MyEditText.Simple">
        <item name="android:background">@color/fui_transparent</item>
    </style>





</resources>
